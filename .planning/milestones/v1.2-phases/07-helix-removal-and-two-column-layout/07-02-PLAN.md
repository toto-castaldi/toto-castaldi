---
phase: 07-helix-removal-and-two-column-layout
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/css/main.css
autonomous: true
requirements: [LAYOUT-06, LAYOUT-07, LAYOUT-08]
must_haves:
  truths:
    - "On desktop (>=1025px) the two project cards display side by side in a 2-column grid"
    - "On mobile (<481px) all cards stack in a single column"
    - "The GitHub profile card spans full width directly below the project cards on desktop"
    - "Design tokens and color values are unchanged, so WCAG AA contrast is preserved in both themes"
  artifacts:
    - path: "assets/css/main.css"
      provides: "Responsive bento grid with 2-column desktop project grid and full-width GitHub cell"
      contains: "repeat(2, 1fr)"
  key_links:
    - from: "assets/css/main.css"
      to: "layouts/index.html"
      via: ".project-cards grid class"
      pattern: "\\.project-cards"
    - from: "assets/css/main.css"
      to: "layouts/index.html"
      via: ".bento-extras full-width GitHub cell"
      pattern: "\\.bento-extras"
---

<objective>
Reflow the project bento grid so the two remaining cards (Docora, Lumio) show in a
2-column layout on desktop instead of the old 3-column layout, while the GitHub
profile card continues to span full width directly below them. Mobile stays single
column and no color tokens change, so WCAG AA contrast is untouched.

Purpose: With Helix gone there are only two project cards; a 3-column desktop grid
would leave an empty column. A 2-column grid presents the two live projects cleanly.
Output: Updated assets/css/main.css with a 2-column desktop project grid, a clean
`hugo --minify` build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@assets/css/main.css
@layouts/index.html

<interfaces>
<!-- Current responsive grid rules the executor must modify precisely. -->
`.project-cards` base rule: `grid-template-columns: 1fr` (mobile, single column).
`@media (min-width: 481px)` sets `.project-cards { grid-template-columns: repeat(2, 1fr); }` (tablet).
`@media (min-width: 1025px)` sets `.project-cards { grid-template-columns: repeat(3, 1fr); }` (desktop — this is the value to change).
`.bento-extras` rule: `grid-template-columns: 1fr` (already full-width; GitHub cell sits below `.project-cards` in DOM order).
Color/contrast tokens live in `:root`, the dark-mode `@media` block, and the
`#dark-toggle:checked` blocks — none of these are touched by this plan.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Change desktop project grid from 3 to 2 columns</name>
  <files>assets/css/main.css</files>
  <read_first>
    - assets/css/main.css (the `.project-cards` base rule and its two media-query overrides at min-width 481px and 1025px)
    - layouts/index.html (confirms `.project-cards` wraps the project articles and `.bento-extras` holds the GitHub cell after it)
  </read_first>
  <action>
    In the `@media (min-width: 1025px)` block, change the `.project-cards`
    `grid-template-columns` value from `repeat(3, 1fr)` to `repeat(2, 1fr)`.
    Leave the base `.project-cards { grid-template-columns: 1fr; }` rule and the
    `@media (min-width: 481px)` `repeat(2, 1fr)` rule unchanged. Do not modify
    `.bento-extras` — it already uses `grid-template-columns: 1fr` (full width) and
    already sits below `.project-cards` in the template, satisfying LAYOUT-07. Do not
    change any color token, spacing token, border, or shadow value — contrast must
    stay identical to preserve WCAG AA in both themes (LAYOUT-08).
  </action>
  <verify>
    <automated>grep -q 'repeat(2, 1fr)' assets/css/main.css && ! grep -q 'repeat(3, 1fr)' assets/css/main.css && hugo --minify --quiet && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'repeat(3, 1fr)' assets/css/main.css` returns no lines (exit code 1)
    - The `@media (min-width: 1025px)` block sets `.project-cards { grid-template-columns: repeat(2, 1fr); }`
    - The base `.project-cards` rule still reads `grid-template-columns: 1fr` and `.bento-extras` still reads `grid-template-columns: 1fr`
    - No line inside `:root`, the dark-mode media block, or `#dark-toggle:checked` blocks (color/border/shadow tokens) is changed
    - `hugo --minify` exits 0
  </acceptance_criteria>
  <done>Desktop shows a 2-column project grid; GitHub card remains full-width below; mobile single column; no color tokens changed; build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo source → GitHub Pages CDN | Static CSS served publicly; no user input, no auth, no runtime code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02 | Tampering | Hugo build / CI pipeline | accept | CSS-only change, zero JavaScript, no new dependencies or package installs; existing CI pipeline unchanged |
| T-07-03 | Denial of Service (accessibility regression) | Color/contrast tokens | mitigate | Plan explicitly forbids touching color tokens; acceptance criteria assert token blocks unchanged, preserving WCAG AA contrast |
</threat_model>

<verification>
- `hugo --minify` exits 0 with no errors.
- `grep 'repeat(2, 1fr)' assets/css/main.css` matches (desktop grid) and `grep 'repeat(3, 1fr)'` returns nothing.
- Rendered pages: two project cards side by side at >=1025px width; GitHub card full width below.
</verification>

<success_criteria>
- Two project cards display side by side on desktop (2-column grid).
- GitHub profile card spans full width directly below the project cards.
- Mobile stacks all cards in a single column; color/contrast tokens unchanged (WCAG AA preserved).
</success_criteria>

<output>
Create `.planning/phases/07-helix-removal-and-two-column-layout/07-02-SUMMARY.md` when done.
</output>
