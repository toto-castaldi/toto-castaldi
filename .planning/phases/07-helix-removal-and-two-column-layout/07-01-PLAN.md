---
phase: 07-helix-removal-and-two-column-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/projects.toml
  - content/_index.it.md
  - content/_index.en.md
  - CLAUDE.md
autonomous: true
requirements: [CONT-07, CONT-08]
must_haves:
  truths:
    - "Visitor on IT and EN site sees only Docora and Lumio cards — no Helix card"
    - "SEO description meta tag lists only Docora and Lumio in both languages"
    - "Case-insensitive search for 'helix' returns zero matches across data, content, i18n, and templates"
  artifacts:
    - path: "data/projects.toml"
      provides: "Project card source data with exactly two [[project]] entries (Docora, Lumio)"
      contains: "Docora"
    - path: "content/_index.it.md"
      provides: "IT front matter with Helix-free description meta"
      contains: "Docora, Lumio"
    - path: "content/_index.en.md"
      provides: "EN front matter with Helix-free description meta"
      contains: "Docora, Lumio"
  key_links:
    - from: "layouts/index.html"
      to: "data/projects.toml"
      via: "range .Site.Data.projects.project"
      pattern: "range .*projects.project"
---

<objective>
Remove the Helix project everywhere it appears in the site source: the data entry
that renders its card, the SEO/description meta in both language front matters, and
the project-list mention in the repo's own CLAUDE.md documentation.

Purpose: Helix is no longer a live project. Visitors must see only Docora and Lumio,
with zero residual Helix references in any shipped artifact.
Output: Helix-free data/projects.toml, content/_index.it.md, content/_index.en.md,
and CLAUDE.md; a clean `hugo --minify` build with zero Helix matches in output.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@data/projects.toml
@content/_index.it.md
@content/_index.en.md
@layouts/index.html

<interfaces>
<!-- Data shape consumed by layouts/index.html. Executor should preserve this exact TOML structure. -->
Each project is a `[[project]]` array-of-tables entry with:
  name (string), url (string), logo (string), and a nested [project.description]
  table holding `it` and `en` string keys.
layouts/index.html renders cards via `range .Site.Data.projects.project` and reads
  `.name`, `.url`, `.logo`, and `index .description site.Language.Lang`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the Helix entry from project data</name>
  <files>data/projects.toml</files>
  <read_first>
    - data/projects.toml (current state: three [[project]] entries — Docora, Lumio, Helix)
    - layouts/index.html (confirms the range loop that consumes each entry)
  </read_first>
  <action>
    Delete the entire third [[project]] block for Helix — the `[[project]]` header,
    its `name = "Helix"`, `url = "https://helix.toto-castaldi.com"`,
    `logo = "https://helix.toto-castaldi.com/logo.svg"` lines, and the nested
    `[project.description]` table with `it = "L'AI al servizio del fitness Coach"`
    and `en = "Coach fitness AI assistant"`. Leave the Docora and Lumio entries
    exactly as-is (do not reorder, rename, or alter their url/logo/description values).
    The file must end with exactly two `[[project]]` entries.
  </action>
  <verify>
    <automated>test "$(grep -c '^\[\[project\]\]' data/projects.toml)" = "2" && ! grep -qi 'helix' data/projects.toml && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^\[\[project\]\]' data/projects.toml` outputs exactly `2`
    - `grep -i 'helix' data/projects.toml` returns no lines (exit code 1)
    - `grep -c 'Docora' data/projects.toml` and `grep -c 'Lumio' data/projects.toml` each return at least 1
  </acceptance_criteria>
  <done>data/projects.toml contains only the Docora and Lumio entries with no Helix reference.</done>
</task>

<task type="auto">
  <name>Task 2: Remove Helix from SEO descriptions and repo docs</name>
  <files>content/_index.it.md, content/_index.en.md, CLAUDE.md</files>
  <read_first>
    - content/_index.it.md (front matter `description` currently ends "Docora, Lumio e Helix")
    - content/_index.en.md (front matter `description` currently ends "Docora, Lumio, and Helix")
    - CLAUDE.md (Project section names three projects and the helix subdomain)
    - layouts/partials/head.html (renders `.Description` into the meta description tag)
  </read_first>
  <action>
    In content/_index.it.md set the front matter `description` to
    `"Progetti software di Toto Castaldi: Docora e Lumio"`.
    In content/_index.en.md set the front matter `description` to
    `"Software projects by Toto Castaldi: Docora and Lumio"`.
    In CLAUDE.md, in the `## Project` section, change the sentence naming
    "three software projects: Docora, Lumio, and Helix" to name
    "two software projects: Docora and Lumio", and change the
    "URL to subdomain (docora/lumio/helix.toto-castaldi.com)" line under `## Content`
    to "(docora/lumio.toto-castaldi.com)". Do not touch the `title` field or any
    other front matter/content.
  </action>
  <verify>
    <automated>! grep -riq 'helix' content/ CLAUDE.md && grep -q 'Docora e Lumio' content/_index.it.md && grep -q 'Docora and Lumio' content/_index.en.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -i 'helix' content/_index.it.md content/_index.en.md CLAUDE.md` returns no lines
    - content/_index.it.md `description` contains the exact string `Docora e Lumio`
    - content/_index.en.md `description` contains the exact string `Docora and Lumio`
    - The `title` line in both content files is unchanged from its prior value
  </acceptance_criteria>
  <done>Both language descriptions and CLAUDE.md name only Docora and Lumio; no Helix text remains.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo source → GitHub Pages CDN | Static content built by CI and served publicly; no user input, no auth, no data store |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | data/projects.toml, content/*.md | mitigate | Removing the Helix entry/mentions ensures no stale project is advertised; verified by zero-match grep and clean build |
| T-07-02 | Tampering | Hugo build / CI pipeline | accept | Static site, no packages installed in this phase; existing GitHub Actions pipeline unchanged, no new dependencies |
</threat_model>

<verification>
- `hugo --minify` exits 0 with no errors.
- `grep -ri 'helix' data/ content/ layouts/ i18n/ 2>/dev/null` returns zero matches.
- `grep -ril 'helix' public/ 2>/dev/null` returns zero matches after a fresh build.
- Rendered `public/it/index.html` and `public/en/index.html` each contain exactly two `project-card` articles.
</verification>

<success_criteria>
- Only Docora and Lumio project cards render on both the IT and EN sites.
- SEO description meta lists only Docora and Lumio in both languages.
- Case-insensitive search for "helix" returns zero matches in data, content, templates, i18n, and built output.
</success_criteria>

<output>
Create `.planning/phases/07-helix-removal-and-two-column-layout/07-01-SUMMARY.md` when done.
</output>
