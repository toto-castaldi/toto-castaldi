---
phase: 07-helix-removal-and-two-column-layout
plan: 03
type: execute
wave: 2
depends_on: [07-01, 07-02]
files_modified: []
autonomous: false
requirements: [CONT-07, LAYOUT-06, LAYOUT-07, LAYOUT-08]
must_haves:
  truths:
    - "Both IT and EN sites show only Docora and Lumio, side by side on desktop, GitHub card full width below"
    - "Mobile stacks all cards in a single column"
    - "Text and border contrast meets WCAG AA in both light and dark themes"
  artifacts: []
  key_links: []
---

<objective>
Final human verification that the Helix removal and 2-column reflow render correctly
across both languages, both themes, and mobile/desktop viewports before shipping v1.2.

Purpose: The layout and contrast criteria (success criteria 3, 4, 5) are visual and
cannot be fully proven by grep/build alone. This checkpoint confirms the shipped
experience matches the goal after the two implementation plans complete.
Output: Human sign-off (or a list of issues to feed into a gap-closure plan).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@layouts/index.html
@assets/css/main.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the site and run automated Helix + grid checks</name>
  <files></files>
  <read_first>
    - assets/css/main.css (confirm desktop grid is repeat(2, 1fr))
    - data/projects.toml (confirm two project entries)
  </read_first>
  <action>
    Run `hugo --minify` to produce a fresh `public/` build. Then run the automated
    guards: confirm zero "helix" matches (case-insensitive) across data/, content/,
    layouts/, i18n/, and public/; confirm each of public/it/index.html and
    public/en/index.html contains exactly two `project-card` article blocks; confirm
    the built CSS contains `repeat(2,1fr)` (whitespace may be minified). Report the
    results so the human reviewer knows the automated gates passed before eyeballing.
  </action>
  <verify>
    <automated>hugo --minify --quiet && ! grep -riq 'helix' data content layouts i18n public && test "$(grep -o 'project-card"' public/it/index.html | wc -l)" -ge 2 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `hugo --minify` exits 0
    - `grep -ri 'helix' data content layouts i18n public` returns no lines
    - public/it/index.html and public/en/index.html each contain exactly two project-card articles
    - Built CSS contains a 2-column grid rule (`repeat(2,1fr)` after minification)
  </acceptance_criteria>
  <done>Fresh build is clean, zero Helix matches, exactly two project cards per language.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify layout, language, and contrast</name>
  <action>
    Pause for the user to visually verify the running site. Present the what-built
    summary and the how-to-verify steps below, then wait for the resume signal.
  </action>
  <what-built>
    Helix removed from all source and rendered output; the desktop project grid
    reflowed from 3 columns to 2 columns; the GitHub profile card spans full width
    below the two project cards. No color tokens changed.
  </what-built>
  <how-to-verify>
    1. Run `hugo server` and open http://localhost:1313/it/ then http://localhost:1313/en/.
    2. Confirm each page shows exactly two project cards (Docora, Lumio) and no Helix
       anywhere (card, text, or link).
    3. At a desktop width (>=1025px), confirm the two project cards sit side by side
       (2 columns) and the GitHub card spans the full width directly below them.
    4. Narrow the browser to mobile width (<481px); confirm all cards stack in a
       single column.
    5. Toggle the dark-mode switch on both IT and EN; confirm text and card borders
       remain clearly legible (WCAG AA contrast) in both light and dark themes.
  </how-to-verify>
  <resume-signal>Type "approved" to ship v1.2, or describe any visual/contrast issue to open a gap-closure plan.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo source → GitHub Pages CDN | Static site verification only; no user input, no auth, no data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Denial of Service (accessibility regression) | Rendered contrast in both themes | mitigate | Human checkpoint explicitly verifies WCAG AA contrast in light and dark themes across IT and EN before ship |
</threat_model>

<verification>
- Automated build + grep gates pass (Task 1).
- Human confirms two-project layout, 2-column desktop grid, full-width GitHub card, single-column mobile, and WCAG AA contrast in both themes across IT and EN.
</verification>

<success_criteria>
- Human sign-off confirms all five phase success criteria are met in the running site.
</success_criteria>

<output>
Create `.planning/phases/07-helix-removal-and-two-column-layout/07-03-SUMMARY.md` when done.
</output>
